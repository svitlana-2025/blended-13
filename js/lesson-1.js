// -------------------Завдання 1:------------------------
// Запропонуйте користувачу ввести число за допомогою prompt().
// Збережіть введене значення у змінну number.
// Перевірте, чи дорівнює змінна number числу 10:
//    Якщо так - виведіть повідомлення 'Вірно' у alert().
//    Якщо ні - виведіть 'Невірно'. 

// Підказка: prompt завжди повертає рядок (String), тому перед перевіркою  
// перетворіть значення на число за допомогою Number().

const number = Number(prompt("Введіть число:"));

if (number === 10) {
  alert("Вірно");
} else {
  alert("Невірно");
}

// --------------------------Завдання 2:-----------------------
// У змінній min лежить випадкове число від 0 до 59.
// Визначте, в яку чверть години потрапляє
// це число (у першу, другу, третю чи четверту).
// Виведіть в alert відповідне повідомлення, наприклад:
// "10 входить в першу чверть"

// const min = Math.floor(Math.random() * (59 - 0) + 0);

const min = Math.floor(Math.random() * 60); // Випадкове число від 0 до 59

let message;

if (min >= 0 && min <= 14) {
  message = `${min} входить в першу чверть`;
} else if (min >= 15 && min <= 29) {
  message = `${min} входить в другу чверть`;
} else if (min >= 30 && min <= 44) {
  message = `${min} входить в третю чверть`;
} else if (min >= 45 && min <= 59) {
  message = `${min} входить в четверту чверть`;
}

alert(message);


// ---------------------Завдання 3:----------------------
// Змінна num може набувати 4 значення: '1', '2', '3' або '4'
// (запитуй це значення у користувача через prompt).
// Якщо вона має значення '1', то у змінну result запишіть 'зима',
// якщо має значення '2' - 'весна' і так далі.
// Розв'яжіть завдання через switch-case.
// Не забудьте про дефолтне значення, на випадок, якщо користувач
// введе в prompt щось інше. В такому випадку result має набувати значення:
// "Вибачте, але ви маєте ввести значення від 1 до 4 включно".
// Значення змінної result виведіть в консоль.

const num = prompt("Введіть число від 1 до 4:");
let result;

switch (num) {
  case '1':
    result = 'зима';
    break;
  case '2':
    result = 'весна';
    break;
  case '3':
    result = 'літо';
    break;
  case '4':
    result = 'осінь';
    break;
  default:
    result = "Вибачте, але ви маєте ввести значення від 1 до 4 включно";
}

console.log(result);

// Пояснення:
// prompt(...) отримує значення від користувача у вигляді рядка.
// switch порівнює num з рядковими значеннями '1', '2', '3', '4'.
// default використовується для обробки будь-якого іншого введення.
// console.log(result) виводить результат у консоль.

// ---------------------Завдання 4:-------------------------
// Отримуйте від користувача число (кількість хвилин) через prompt
// і виводьте у консоль рядок у форматі годин та хвилин.
// Приклад: користувач вводить в prompt '70' -> в консолі відобразиться '01:10'.
// Корисне посилання для відображення годин та хвилин у потрібному форматі ('01' замість '1'):
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/padStart#examples

const totalMinutes = Number(prompt("Введіть кількість хвилин:"));

const hours = Math.floor(totalMinutes / 60);
const minutes = totalMinutes % 60;

const formattedHours = String(hours).padStart(2, '0');
const formattedMinutes = String(minutes).padStart(2, '0');

console.log(`${formattedHours}:${formattedMinutes}`);

// Приклад:
// Користувач вводить: 70
// У консолі буде: 01:10
// Пояснення:
// Math.floor(...) — визначає кількість повних годин.
// totalMinutes % 60 — залишок хвилин.
// padStart(2, '0') — додає провідний нуль, якщо значення менше 10 (тобто 9 → 09).

// ----------------Завдання 5:-----------------
// Напишіть код, який запитуватиме у користувача
// логін за допомогою prompt і виводить результат в консоль браузера

// Після цього додайте перевірку введеного користувачем значення:
// Якщо відвідувач вводить "Адмін",
// то prompt запитує пароль (не забудьте зберігти його у змінну для подальшої перевірки).
// Якщо нічого не ввели або натиснули Cancel,
// Вивести в alert строку "Скасовано"
// В іншому випадку вивести в alert рядок "Я вас не знаю"

// Пароль перевіряти так:
// Якщо введено пароль "Я головний",
// то вивести в alert рядок "Добрий день!",
// в іншому випадку вивести в alert рядок "Невірний пароль!"

const login = prompt("Введіть логін:");
console.log(`Користувач ввів логін: ${login}`);

if (login === "Адмін") {
  const password = prompt("Введіть пароль:");

  if (password === "Я головний") {
    alert("Добрий день!");
  } else if (password === null || password === "") {
    alert("Скасовано");
  } else {
    alert("Невірний пароль!");
  }

} else if (login === null || login === "") {
  alert("Скасовано");
} else {
  alert("Я вас не знаю");
}

// Логіка:
// Якщо користувач ввів "Адмін" — запитуємо пароль.
// Якщо пароль правильний — "Добрий день!", інакше — "Невірний пароль!".
// Якщо логін або пароль не введені (порожній рядок або Cancel) — "Скасовано".
// Якщо логін не "Адмін" — "Я вас не знаю".

// -----------------------Завдання 6:----------------------
// Використайте цикл while, щоб вивести в console всі числа від 0 до 20 включно.
let i = 0;

while (i <= 20) {
  console.log(i);
  i++;
}

// Пояснення:
// let i = 0 — початкове значення лічильника.
// while (i <= 20) — цикл виконується, поки i не перевищує 20.
// i++ — збільшує значення i на 1 після кожної ітерації.

// -----------------------Завдання 7-----------------------
// Напишіть функцію getNumbers(min, max), що приймає 2 параметри -
// мінімальне і максимальне число відповідно.
// Напишіть цикл всередині функції, який виводить у консоль
// всі числа від max до min за спаданням.
// Окрім цього, підрахуйте суму всіх парних чисел в цьому циклі
// і поверніть її з функції.

function getNumbers(min, max) {
  let sum = 0;

  for (let i = max; i >= min; i--) {
    console.log(i);

    if (i % 2 === 0) {
      sum += i;
    }
  }

  return sum;
}
// const result = getNumbers(3, 10); // Виведе в консоль: 10, 9, ..., 3
// console.log("Сума парних чисел:", result); // Виведе: Сума парних чисел: 28

// Пояснення:
// Цикл for рахує від max до min, зменшуючи значення.
// if (i % 2 === 0) перевіряє, чи число парне.
// Парні числа додаються до змінної sum, яка повертається з функції.

// ----------------------Завдання 8--------------------
// Напишіть функцію man(a, b), яка приймає 2 числа і повертає меньше з них.
// Потрібно додати перевірку, що функція отримує саме числа, в іншому випадку
// повертати з функції рядок - 'Not a number!'.

function man(a, b) {
  if (typeof a !== 'number' || typeof b !== 'number') {
    return 'Not a number!';
  }

  return a < b ? a : b;
}
console.log(man(5, 8));        // 5
console.log(man(10, -3));      // -3
console.log(man('5', 2));      // Not a number!
console.log(man(7, 'три'));    // Not a number!

// Пояснення:
// typeof перевіряє, чи обидва аргументи є числами.
// Якщо так — повертається менше з них.
// Якщо ні — повертається повідомлення 'Not a number!'.

// --------------------Завдання 9------------------
// Напишіть функцію isAdult(age), яка  приймає число - вік користувача і повертає true, 
// якщо параметр age більше чи дорівнює 18.
// В іншому випадку вона запитує підтвердження через confirm 
// і повертає його результат (true/false).
function isAdult(age) {
  if (age >= 18) {
    return true;
  } else {
    return confirm("Вам менше 18. Ви впевнені, що хочете продовжити?");
  }
}
console.log(isAdult(20)); // true
console.log(isAdult(16)); // відкриється confirm, результат залежить від вибору користувача

// Пояснення:
// Якщо age >= 18, функція одразу повертає true.
// Інакше з'являється confirm(...), і результат цього підтвердження повертається з функції.


//----------------------------Завдання 10--------------------------
// Напишіть функцію fizzBuzz(num), яка приймає число і перевіряє кожне число від 1 до num:
// Якщо число ділитися  без остачі на 3 - виводить в консоль 'fizz',
// якщо ділиться  без остачі на 5 - виводить в консоль 'buzz',
// якщо ділиться  без остачі і на 3, і на 5 - виводить в консоль 'fizzbuzz'.
function fizzBuzz(num) {
  for (let i = 1; i <= num; i++) {
    if (i % 3 === 0 && i % 5 === 0) {
      console.log('fizzbuzz');
    } else if (i % 3 === 0) {
      console.log('fizz');
    } else if (i % 5 === 0) {
      console.log('buzz');
    } else {
      console.log(i);
    }
  }
}
// Приклад виклику
// fizzBuzz(15);
// Пояснення:
// Перевірка спочатку на подільність одночасно на 3 і 5 (тобто на 15).
// Далі окремо на 3 та на 5.
// Якщо жодна умова не виконується — виводиться саме число.